console.log('start');

var game = new Phaser.Game(800, 600, Phaser.CANVAS, 'container', { preload: preload, create: create });
console.log(game);

	function preload() {
			}

	
			function create() {
			}

			function update() {

			}
